# 有善商户端微信小程序

Taro 4 + React 18 + TypeScript。

## 启动

```bash
npm install
npm run dev:h5
```

浏览器打开：http://localhost:10086 （若端口占用会自动改用 10087/10088 等，以终端输出为准）

微信小程序：

```bash
npm run dev:weapp
```

用微信开发者工具打开项目 `dist` 目录。
